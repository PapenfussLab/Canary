#	Canary shell wrapper

#	Set to install directory of Canary
#
CANARY_HOME=${CANARY_HOME:-/canary/install/dir}

JAVA_OPTS="-XX:-UseSplitVerifier \
-Dpathos.config=$CANARY_HOME/etc/canary.properties \
-Dpathos.home=$CANARY_HOME \
-Dpathos_mutalyzer_waittime=1000 \
-Dlog4j.configuration=file:$CANARY_HOME/lib/log4j.properties"

java $JAVA_OPTS -cp ${CANARY_HOME}/lib/Canary-all-1.0.0.jar org.petermac.pathos.pipeline.Canary $*
